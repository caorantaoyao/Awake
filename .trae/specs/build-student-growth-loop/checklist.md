- [x] 注册和登录以外的学生业务接口均要求有效 Bearer Token
- [x] 当前学生不能读取、修改或完成其他学生的任务、画像、消息和成长事件
- [x] 客户端提交的 email、student_id 和 student_name 不能改变服务端识别的学生身份
- [x] 普通学生不能访问或修改 DeerFlow skill/model 控制接口
- [x] 旧 SQLite 数据库可通过幂等迁移新增画像、消息、资源、成长事件和任务字段
- [x] 每个学生拥有结构稳定的成长画像，包含兴趣、能力、阶段、摘要和更新时间
- [x] 学生可以查看并修正自己的画像，空画像有明确引导
- [x] 对话消息按学生持久化，刷新后恢复且不重复欢迎消息
- [x] 三轮探索后可以生成结构化微行动，时长为 5-30 分钟且成长值为正整数
- [x] DeerFlow 不可达或输出非法时，对话、画像和任务均使用确定性降级且不返回 500
- [x] 今日接口稳定选择一个优先任务，多任务不会丢失
- [x] 任务完成幂等，重复提交不重复累计成长值或成长事件
- [x] AI Focus 支持开始、暂停、继续、结束和刷新恢复
- [x] `prefers-reduced-motion` 下 AI Focus 和页面入场动效不会循环位移或缩放
- [x] Explore 使用受控资源目录，支持课程、活动、竞赛、实践筛选
- [x] 推荐排序可解释、确定，无画像时不伪造个性化结论
- [x] Growth Map 只展示真实任务和画像事件，不生成不存在的经历
- [x] 最近七天摘要由真实任务数据计算，包含创建数、完成数和累计成长值
- [x] DeerFlow 新增 `awaken-student-growth` Skill，未修改 Gateway、Harness 或模型路由
- [x] Skill 包含有效 frontmatter、中文规则、JSON 契约和未成年人教育伦理边界
- [x] 学生一级导航为今日、对话、微行动、探索、成长，账号设置为工具入口
- [x] `/app/capabilities` 不再对普通学生展示或开放
- [x] 今日首页聚合今日任务、画像摘要、推荐资源和最近成长
- [x] 对话页保留 5 秒/15 秒等待文案、取消和失败重试
- [x] 微行动页使用进行中优先的纵向结构，不使用挤压的三栏看板
- [x] Explore 和 Growth 均实现互斥的加载、空、错误和成功状态
- [x] 桌面 1366×768 下主要动作首屏可见且无横向滚动
- [x] 移动 390×844 与 320×568 下为单栏，无横向滚动，触控目标至少 44×44px
- [x] 工作台使用单一湖蓝强调色、统一圆角和清晰焦点态，不展示 gateway、provider、assistant_id 等工程词
- [x] 未新增 UI 库、状态管理库、字体包或动画依赖
- [x] 后端新增功能均有先失败后通过的单元测试证据
- [x] 前端核心纯逻辑均有先失败后通过的 Node 单元测试证据
- [x] 后端全量 pytest 通过
- [x] 前端单元测试通过
- [x] `cd frontend && npm run build` 通过
- [x] curl 测试覆盖注册、登录、画像、对话、今日任务、资源、打卡和成长时间线
- [x] 浏览器端到端完成“登录 → 对话 → 生成任务 → 专注 → 打卡 → 查看成长”路径
- [x] 浏览器控制台无本次变更新增 error
- [x] Ponytail 最终审查通过：无无调用代码、重复 helper、无依据兜底、无关重构
